"use client";

/**
 * Custom Cursor disabled for maximum performance and native OS responsiveness.
 * Hiding the native cursor causes lag, stuttering, and poor user experience on high-refresh-rate displays.
 */
export function CustomCursor() {
  return null;
}
